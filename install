sudo yum install -y docker
sudo systemctl enable docker 
sudo service docker start
sudo usermod -a -G docker ec2-user
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
sudo yum install git -y
git clone https://github.com/Mohan14242/GITHUB-SSO.git



add the production deployment  after approval need to trigger and also show the pipeline viewer-done  

when the deploy pipeline deployment happening to the specific environment at that disable rollback button to that specific environement


add the rollback to the service dashboard and also add the pipeline viwer to it 

next add the missing version tempates to the templates 

add github deployment also 

add the nice ui deployment approvals  and rollback 

in the service dashboard i need to see the how many deployments happen to the each environment environment success and failures and rollbacks in each environement 


