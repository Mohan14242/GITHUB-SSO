sudo yum install -y docker
sudo systemctl enable docker 
sudo service docker start
sudo usermod -a -G docker ec2-user

sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose

sudo chmod +x /usr/local/bin/docker-compose





add the rollback to the service dashboard and also add the pipeline viwer to it 

add the production deployment  after approval need to trigger and also show the pipeline viewer


next add the missing version tempates to the templates 


add github deployment also 


add the nice ui deployment approvals 



